# Session Service 

## Run
* in dev mode
```make dev``` or ```docker run -d -p 9000:8000 chromadb/chroma && fastapi dev main.py```
* in prod mode
```make prod``` or ```docker compose up```
* API documentation
```http://localhost:8000/docs``` (dev) or ```http://localhost:8080/docs``` (prod)
